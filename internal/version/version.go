package version

// DawarichCompat is advertised in X-Dawarich-Version so official clients
// treat this ingest as a 1.14.1-compatible endpoint set.
const DawarichCompat = "1.14.1"

// Version is itayo's own release identifier.
const Version = "0.1.0"
